// const statsInnerBack = document.querySelector('.statsInnerBack');
// const upTriangle = window.getComputedStyle(statsInnerBack, '::before');
// const loTriangle = window.getComputedStyle(statsInnerBack, '::after');

// function resize() {
//     statsInnerBack.style.setProperty('--windowSize', window.innerWidth + 'px');
//     loTriangle.borderLeft = window.innerWidth + 'px solid transparent';
// }

// resize();

// document.documentElement.style.setProperty('--windowWidth', window.innerWidth + 'px');

// document.documentElement.style.setProperty('--proba', 'green');